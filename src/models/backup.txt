import { WithId, ObjectId, Document } from "mongodb";

export default class Game {
    constructor(public _id: ObjectId, public name: string, public price: number, public category: string, public id?: ObjectId) {}
}
